import sys
import os

if len(sys.argv) >= 2:
  for file in sys.argv[1:]:
	  	file = open(sys.argv[i], 'r')
		  print(''.join(filel.readlines())
else:
	print('At least 1 file must be specified')
	exit(-1)
